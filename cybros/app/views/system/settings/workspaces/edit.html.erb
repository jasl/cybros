<div class="space-y-6">
  <div class="flex items-center justify-between gap-4">
    <div>
      <h2 class="text-lg font-semibold">Edit Workspace</h2>
      <div class="text-sm opacity-70"><%= @workspace.name %></div>
    </div>
  </div>

  <%= render "form", workspace: @workspace %>
</div>
