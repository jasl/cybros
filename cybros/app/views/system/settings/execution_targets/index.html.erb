<div class="space-y-6">
  <div class="flex items-center justify-between">
    <h2 class="text-lg font-semibold">Execution Targets</h2>
    <%= link_to "Manage locations", system_settings_execution_locations_path, class: "btn btn-ghost btn-sm" %>
  </div>

  <div class="card bg-base-200/30 border border-base-300 shadow-sm">
    <div class="card-body p-0 overflow-x-auto">
      <table class="table">
        <thead>
          <tr>
            <th>Name</th>
            <th>Location</th>
            <th>Workspace</th>
            <th>Status</th>
            <th></th>
          </tr>
        </thead>
        <tbody>
          <% @execution_targets.each do |target| %>
            <tr class="hover:bg-base-200/50 transition-colors">
              <td class="font-medium"><%= target.name %></td>
              <td><%= link_to target.execution_location.name, system_settings_execution_location_path(target.execution_location), class: "link link-hover" %></td>
              <td class="font-mono text-xs"><%= link_to target.workspace.root_path, system_settings_workspace_path(target.workspace), class: "link link-hover" %></td>
              <td><%= target.status %></td>
              <td class="text-right">
                <%= link_to "View", system_settings_execution_target_path(target), class: "btn btn-ghost btn-sm" %>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>
