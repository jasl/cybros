import { test } from "@playwright/test"

test.describe("Conversation execution progress", () => {
  test.skip("renders structured execution progress for running assistant bubbles", async () => {
    // Covered by integration tests for now. Keep this placeholder so the E2E path
    // can be promoted once a deterministic tool-calling fixture is available.
  })
})
