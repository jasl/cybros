# Archived Plans

This directory holds historical plans that are not part of the current programmable-agent deployment rebaseline thread.

They are kept for reference only and should not be treated as active implementation guidance.

Current live plans remain in `docs/plans/`.

If an archived document contains old `docs/plans/...` references, read them as historical references from before the archive move or use git history to recover the original context.
